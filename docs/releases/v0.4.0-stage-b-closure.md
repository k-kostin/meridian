# v0.4.0 Stage B Closure

Date: 2026-06-08

## Purpose

Close Stage B operational contour work as a milestone release.

## Included Slices

- Stage B scope reconciliation after Stage A closure.
- Import column aliases for item import and opening inventory import.
- Explicit item import update mode for existing items.
- Explicit unit auto-create mode during item import.
- Item categories and category filter in the item list.
- Category-aware operational filters for balances, daily ledger, monthly ledger, period analytics, and matching Excel exports.
- User saved views for movement documents and current balances.

## Versioning

- Application version: `v0.4.0`.
- Electron package version: `0.4.0`.

## Verification Gate

The closure gate must pass:

- `manage.py check`
- `manage.py test`
- `scripts/check_changed.py --full`
- `scripts/check_public_readiness.py`
- `python -m unittest discover -s scripts -p 'test_check_public_readiness.py'`
- `git diff --check`

## Do Not Regress

Preserve `v0.1 MVP Baseline` and `v0.3.0 Stage A Closure` invariants.

Stage B features must remain opt-in where specified: import update mode and unit auto-create mode must not change the default create-only import behavior.
