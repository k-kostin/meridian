# v0.2 Foundation

Date: 2026-06-03

## Purpose

Start the next-version line without changing warehouse accounting behavior.

This slice adds explicit product version metadata so future desktop builds, UI, documentation, and release notes can stay aligned.

## Changes

- Added `warehouse_app/version.py` as the single application version source.
- Exposed the version label through the shared template context.
- Displayed `v0.2.0` in the sidebar.
- Updated Electron package version to `0.2.0`.
- Documented the versioning convention.

## Do Not Regress

This version must preserve the `v0.1 MVP Baseline` behavior documented in `docs/releases/v0.1-mvp-baseline.md`.

## Verification

Required before merging:

```bash
python manage.py check
python manage.py test
python scripts/check_changed.py --full
```
