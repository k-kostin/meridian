{
  "name": "warehouse-control-desk",
  "version": "0.5.0-rc.1",
  "private": true,
  "description": "Windows desktop shell for Warehouse Control Desk.",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "check:contract": "node scripts/check-packaging-contract.js",
    "dist:win": "electron-builder --win nsis",
    "pack:win": "electron-builder --win dir"
  },
  "devDependencies": {
    "electron": "43.2.0",
    "electron-builder": "26.15.3"
  },
  "build": {
    "appId": "ru.warehouse.control-desk",
    "productName": "Warehouse Control Desk",
    "asar": true,
    "directories": {
      "output": "dist"
    },
    "files": [
      "src/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "resources/backend",
        "to": "backend",
        "filter": [
          "**/*"
        ]
      }
    ],
    "win": {
      "target": [
        "nsis"
      ]
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowElevation": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true
    }
  }
}
